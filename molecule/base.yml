---
dependency:
  name: galaxy
  options:
    role-file: requirements.tests.yml
driver:
  name: docker
lint:
  name: yamllint
provisioner:
  name: ansible
  lint:
    name: ansible-lint
verifier:
  name: testinfra
  lint:
    name: flake8
